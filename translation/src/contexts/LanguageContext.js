import React from 'react';

// purpose of file is to create Context Object and export it
	// Placed in separate file to only import into files that need information

export default React.createContext('english');